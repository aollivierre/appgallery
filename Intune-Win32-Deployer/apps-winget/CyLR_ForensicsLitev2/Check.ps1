$archiveFolderPath = "C:\Intune2\Win32\cylr2"; $minimumFileSize = 1MB; if ((Get-ChildItem -Path $archiveFolderPath -Filter "*cylr_archive.zip*" | Where-Object { $_.Length -gt $minimumFileSize }).Count -gt 0) { Write-Output "Found $((Get-ChildItem -Path $archiveFolderPath -Filter "*cylr_archive.zip*" | Where-Object { $_.Length -gt $minimumFileSize }).Count) CyLR archive file(s) larger than 1 MB in the folder $archiveFolderPath"; exit 0 } else { exit 1 }